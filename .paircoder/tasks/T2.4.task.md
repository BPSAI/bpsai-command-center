---
id: T2.4
title: Session catalog UI panel
plan: plan-2026-03-cc-s4
type: feature
priority: P0
complexity: 10
status: pending
sprint: "CC-S4"
tags: [ui, sessions]
depends_on: [T2.3]
trello_card_id: "26"
---

# Objective

Add a session catalog view to Command Center showing dispatched daemon sessions with status, timing, and operator info.

# Implementation Plan

- Session list component fetching from `GET /sessions`
- Status badges with color coding
- Auto-refresh on interval (30s)
- Click-to-detail with command, timing, output summary
- Filter by operator (auto-filtered to current user by default)

# Acceptance Criteria

- [ ] Session list component fetching from `GET /sessions`
- [ ] Status badges (started=yellow, running=blue, complete=green, failed=red)
- [ ] Auto-refresh on interval (30s) or SSE when available
- [ ] Click session to see detail (command, timing, output summary)
- [ ] Filter by operator (auto-filtered to current user by default)
- [ ] Responsive layout — fits in existing panel grid or as modal overlay

# Verification

- Sessions from daemon visible in panel with correct status colors
- Click session → detail view shows command and timing
- Auto-refresh picks up new sessions without manual reload
